name: vapa_app
description: Voice-Activated Personal Assistant Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  speech_to_text: ^7.3.0
  flutter_tts: ^4.0.1
  #flutter_local_notifications: ^17.2.2
  provider: ^6.1.2
  intl: ^0.19.0
  uuid: ^4.4.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true